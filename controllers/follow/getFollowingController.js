const db = require('../../db')




const getFollower = (FollowedId) => {
    let follwer = db('User')
    followe
    let query = db('User')
    
    return query
        .select(FollowerId, 'Follower.avatar', 'Follower.name', 'User.bio', 'Follower.background', 'Follower.')
      
        let query = db('User')
        
        .where 
      
        .andWhere('User.userId', 'Follow.follower_id')
        .leftJoin('Follow', 'Follow.Followed_id', userId)
        .as('Follower')
        .andWhereRaw()
        
        knex({})
        .select()
        .where()
        andWhereRaw()
}

const alias = () => {

    return knex({follower: 'User.use
    
    }

}



